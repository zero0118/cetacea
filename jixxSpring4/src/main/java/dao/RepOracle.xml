<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kitri.project.repository.Mapper">
	<resultMap type="vo.Repository" id="RepResultMap">
		<result property="rep_id" column="rep_id" />
		<result property="rep_name" column="rep_name" />
		<result property="rep_url" column="rep_url" />				
	</resultMap>
	<select id="getRepository" parameterType="vo.Repository" resultMap="RepResultMap">
	select * from repository where rep_url=#{rep_url}
	</select>
	<insert id="insertRep" parameterType="vo.Repository">
	insert into repository values((select max(rep_id)+1 from repository), #{rep_name}, #{rep_url}
	</insert>
	

	<insert id="insert"
		parameterType="vo.Repository">		
		 insert into member values((select max(user_id)+1 from member),#{email},#{pwd},#{name})
	</insert>

	<update id="update"
		parameterType="vo.Repository">
		update member
		set
		pwd=#{pwd}, name=#{name}, email=#{email} where id=#{id}
	</update>

	<delete id="delete" parameterType="String">
		delete member where
		id=#{id}
	</delete>
</mapper>